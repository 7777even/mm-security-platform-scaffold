# AGENTS.md — 文档目录职责

本仓库有三类"写东西的地方"，职责不重叠。放错地方的知识等于丢掉。

| 目录           | 回答的问题           | 内容特征                             |
| -------------- | -------------------- | ------------------------------------ |
| `docs/`        | 系统**现在**是什么样 | 长期共识，跨版本有效，改了要同步代码 |
| `openspec/`    | 系统**将要**怎么变   | 唯一业务规格来源，含 change 与 specs |
| `engineering/` | 这次**做得怎么样**   | 短期过程记录：计划、QA、复盘         |

**短期开发记录放 `engineering/`，不放 `docs/`。** QA 结果、发布检查、复盘不写进 `docs/`。

## docs/ 现有内容

| 路径                         | 说明                                                      |
| ---------------------------- | --------------------------------------------------------- |
| `UI规范-大屏端.md`           | 大屏端必读（`:root`），改动大屏前必读                     |
| `UI规范-后台管理端.md`       | 后台端必读（`data-theme="mgmt"`）                         |
| `UI规范-移动端.md`           | 移动端必读（`data-theme="mobile"`）                       |
| `详细设计V1.5偏差分析.md`    | 详设与本仓库实现的偏差说明，含 hybrid 集成待确认项        |
| `perf/`                      | 性能基线与验收记录                                        |
| `superpowers/`               | 工程方法沉淀                                              |
| `安全管控平台设计说明*.docx` | 原始设计说明（二进制原件）                                |
| `_extracted_media/`          | docx 解包抽出的媒体文件，**不是规范资产**，不要引用其内容 |

## 改完代码后的三查

功能完成后必须检查并同步：

1. **契约是否更新**——接口字段、权限码、错误码变化是否落到契约与调用方。
2. **架构是否变化**——目录职责、模块边界、端间共享关系是否变了。
3. **构建 / 部署是否变化**——入口、环境变量、构建脚本、部署配置是否受影响。

三者都落在 `docs/`（长期共识）；本次验证的过程与证据落在 `engineering/qa/`。

## 相关规则

- 规则入口与执行流程：根 `../AGENTS.md` §1–§9。
- 分层规范：`../apps/mgmt/AGENTS.md`、`../apps/mobile/AGENTS.md`、`../src/screen/AGENTS.md`、`../subapps/AGENTS.md`。
- 过程记录规范：`../engineering/README.md`。
