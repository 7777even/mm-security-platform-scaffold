# E2E 专用环境变量（被 playwright.config.ts 的 webServer 以 `--mode e2e` 加载）
# 目的：彻底绕开 .env.development 里的 8787，强制走隔离验证后端 8899，并启用 dev 凭据自动登录。
VITE_API_BASE=http://localhost:8899/api/v1
VITE_USE_DEV_MOCK=false
VITE_DEV_USERNAME=admin
VITE_DEV_PASSWORD=admin@2026
