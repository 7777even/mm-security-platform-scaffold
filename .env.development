# 开发环境对接 B3 Mock 服务（mock/ 目录，默认端口 8787）
VITE_API_BASE=http://localhost:8787/api/v1
VITE_WS_BASE=ws://localhost:8787/ws
# 实时告警 WS：**故意不设置 VITE_ALARM_WS_URL**，走 realtime.ts 的默认相对地址 /ws/alarm。
# 原因（2026-09-20 修正）：wujie 子应用由 `npm run build:subapps` 预打 IIFE 包，WS 地址在构建期
# 被内联（只读 .env.production），子应用内恒为相对 "/ws/alarm"。若主壳用绝对地址
# （旧值 ws://localhost:8787/ws/alarm）而子应用用相对地址，dev 下两条路径走向不同，只修一侧必留坑
# —— 子应用的相对地址会落到 dev server 的 SPA 兜底 HTML 上，握手永不完成（控制台 WS failed）。
# 现统一走相对地址：dev 由 vite.config.ts 的 server.proxy['/ws'] 反代到 8787（target 随 mode 变化，
# e2e→8899），与生产 nginx 的 `location /ws/` 完全同构。如需临时直连后端，取消下一行注释即可
# （仅对 dev 主壳 / mgmt / mobile 生效，预包子应用无效）：
# VITE_ALARM_WS_URL=ws://localhost:8787/ws/alarm

# 开发期前端自包含 mock 开关：后端（B3 :8787）未启动时设为 true，前端用内置 fixture 演示。
# 现后端已在 8787 提供真实接口，故置 false —— main.ts 仅在 true 时给 axios 装全局 mock 适配器拦截所有 REST。
VITE_USE_DEV_MOCK=false

# 脚手架阶段 dev 自动登录凭据：对齐后端 AuthService.ensureAdmin() 写入的默认账号 admin / admin@2026。
# 真实模式（mock=false）下 main.ts 用其调用 POST /auth/login 换取 JWT 并写入内存态（§5.3）。
# 仅存在于 .env.development；生产环境必须由 IDP SSO 下发令牌，前端不得持有任何口令。
VITE_DEV_USERNAME=admin
VITE_DEV_PASSWORD=admin@2026

# 开发态地图预览瓦片：公网 Carto 深色底图（你之前看到的真实底图）。
# loadBaseMap 会在加载前探测可达性：可达→真实底图；不可达（如本环境无外网）→自动回退离线网格底图，不刷屏。
# 生产由 .env.production 控制（未设置 → 走 /tiles 同源离线，符合 S1 §9.4）。
VITE_MAP_TILE_URL=https://basemaps.cartocdn.com/dark_all/{z}/{x}/{y}.png

# 降雨雷达可选全球源（RainViewer）：部分网络对其瓦片要求 API Key（返回"API key required"报错瓦片）。
# 默认走中央气象台免密雷达（nmc.cn，开箱即用）；如需全球动画视图，去 rainviewer.com 免费注册 key 后填这里。
# VITE_RAINVIEWER_KEY=
#
# RainViewer 元数据（weather-maps.json）经同源反向代理拉取，浏览器不再直连公网、规避 CORS：
#   dev 由 vite.config.ts server.proxy 的 /rainviewer-api 反代；生产由 deploy/nginx.conf 同路径反代。
# 如需改走自有网关/直连，可覆盖：VITE_RAINVIEWER_API_BASE=https://<your-gateway>/rainviewer
# VITE_RAINVIEWER_API_BASE=

# 防重放签名旁路（详设 V1.5 §3.3）：开发联调 B3 Mock 不校验签名，挂起；
# 生产环境（.env.production）必须为 false，由网关强制校验 X-Timestamp/X-Nonce/X-Signature。
VITE_SECURITY_GATEWAY_BYPASS=true
