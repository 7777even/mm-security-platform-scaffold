---
description: OpenAPI 契约编写与跨端同步写法（前端视角）——契约真源在本库 docs/api、四条铁律、四同步不可跳步、契约侧必须先绿先推。任何新增/修改对外接口、字段、错误码、权限前必读。
alwaysApply: true
---

# API 契约编写与同步（前端视角）

（提炼自 `templates/api-contract-writing-guide.md` + 本库 `AGENTS.md` §3；完整口径以 `templates/api-contract-writing-guide.md` 为准。）

## 1. 契约真源在本库

- 机器可读契约**唯一真源在** `docs/api/<domain>.openapi.json`；本库由它 `npm run gen:api-types` 生成 `src/types/generated/**`。
- 后端库只做实现对齐，不复制第二份；**前端不得自行设计接口、字段或权限**——契约先行，后端必须真实校验认证与授权。

## 2. 四条铁律（改契约时逐条自检）

1. **按域分组**（`tags` + Controller 分组）：一域一文件。
2. **接口有注释**：每个 operation 有 `summary` + `description`（含权限、规则、主要错误码）。
3. **字段有中文 `description`**：每个 property / parameter 均需中文说明。
4. **有 example**：参数 example、有 body 时请求体 example、成功响应 `code`/`message`/`data` example。

校验入口：`node scripts/validate-api-contracts.mjs`（CI 门禁，铁律缺失即失败；二进制响应有窄豁免，JSON 分支缺 example 仍报错）。

## 3. 四同步（不可跳步，同一次交付内完成）

1. **OpenSpec**：建 `openspec/changes/<name>/`。
2. **契约（本库）**：改 `docs/api/<domain>.openapi.json`（守 §2 四铁律）。
3. **后端实现**：后端对齐后跑 `check-api-contract.mjs --strict`。
4. **生成类型**：本库 `npm run gen:api-types`（内置 `prettifyGenerated()`，无需再手动 prettier；验证重新生成后 `src/types/generated/` 工作区 diff 为 0）。

**顺序纪律（重要）**：本库契约必须先改、先绿、先推送，后端 `contract-guard` 才可能通过；契约侧绿后 `notify-backend-contract` 自动向后端发 `repository_dispatch` 重跑对拍。

## 4. 服务层消费红线（摘要）

- 零下行控制（只监不控，硬控路径交 `guardHardControl` 统一拦截）、B3 `unwrapBody<T>` 解包、20 位 MDM 设备编码、生产不旁路 HMAC、令牌不落 localStorage 明文——详见 `.cursor/rules/frontend-scaffold-core.mdc` 与本库 `AGENTS.md` §3。
- `src/services/*.ts` 用手写 TS 接口 + `request()` + 三态取数（`live`/`demo`/`offline`）与类型守卫，与 `src/types/generated/` **分离**，不要混用。

## 5. 禁止清单

- 前端自行发明接口 / 字段 / 权限；只改契约不通知后端；契约未推送就先推前端消费侧改动；用 Markdown 清单替代 OpenAPI。
