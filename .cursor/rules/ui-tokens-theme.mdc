---
description: 前端脚手架的 UI/主题硬性红线——禁止硬编码设计 token、状态着色只能用规范映射、z-index 只用五层 token、三端主题挂载正确、视觉语言不跨端迁移。任何涉及样式/组件/主题的改动前必读。
alwaysApply: true
---

# UI 与主题红线

设计 token 真源是 `src/styles/tokens.css`，所有视觉取值必须走 `var(--token)`。

## 1. 禁止硬编码

颜色 / 字号 / 间距 / 圆角 / 尺寸一律引用 `src/styles/tokens.css` 的 `var(--token)`；新增或调整 token **只改 `tokens.css` 单一真源**，禁止在组件内重定义。

## 2. 状态/等级/设备着色

状态、报警等级、设备状态的着色**只用 `docs/UI规范-*.md` 中的映射表**（枚举可直接复制），禁止自造色阶或文案。

## 3. z-index 五层

只用：`--z-base(0) / --z-marker(5) / --z-chrome(10) / --z-overlay(30) / --z-toast(40)`。

## 4. 主题挂载

- 大屏可视化端：`:root`，**不挂** `data-theme` 属性。
- 后台管理端：`data-theme="mgmt"`。
- 移动端：`data-theme="mobile"`。
- 组件内不写端特异硬编码分支，用 token 自然区分。

## 5. 状态标签

用全局 `.tag-success / -warning / -danger / -info` 类（后台/移动浅底同色深字；大屏直接用语义色）。

## 6. 视觉语言不跨端

- 玻璃质感 / 发光边框 / 渐变 / 科技青 `#00d8ff` / 深蓝黑底：**仅限大屏**。
- 浅色白卡 / 浅底标签 / 实色按钮（`#0b69d7` 后台、`#1677ff` 移动胶囊）：**仅限后台与移动**。
- 大屏禁止白底卡片、浅灰页面底、`#0b69d7`、`#1677ff`、`.tag-*` 浅底标签；后台/移动禁止玻璃、`#00d8ff`、发光、深蓝黑底。

## 7. 移动端特殊项

- 触控热区 48–56px；底部主操作条预留 `var(--mb-bottom-safe)`。
- 户外强光：`data-skin="outdoor"` 纯黑白高反差（WCAG AAA，对比 >7:1），停用渐变/投影/毛玻璃/浅灰线；状态标签改高饱和纯色块 + 白字 + 2px 黑硬描边。
- 适老：只放大字号/控件/间距（行高覆写 `--mb-line-height`），不改变流程与字段。

## 8. 存量例外（src/screen）

`src/screen/**` 为 fire-monitoring 迁入存量：规范/token 已覆盖项必须用 `var(--token)`；未覆盖项可沿用 `src/screen/styles/variables.css` 源体系，新增页面不得新增未入 token 的硬编码值。

## 9. 品牌基线（中石化 / 石化智云 UI 规范）

- Logo 置左上角回主页；中文默认微软雅黑优先；表格/列表开斑马纹（`--row-alt-bg`）。
- 代码统一 2 空格缩进；根 HTML 必须 `<!DOCTYPE html>` + `UTF-8`。
- 偏离需回对应端 `docs/UI规范-*.md` 确认，不得擅自降级。

> 改任何端 UI 前，先读 `docs/UI规范-大屏端.md` / `UI规范-后台管理端.md` / `UI规范-移动端.md`。
