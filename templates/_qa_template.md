# QA — <变更名称（一句话）>

- 日期:<YYYY-MM-DD>
- 效率等级:<L3 / L4>（对应改动等级）
- 范围:<受影响的端 / 文件 / token，列举关键路径与受影响目标。>

## 验收口径

- <逐条列出验收标准，引用 AGENTS.md §5 DoD / §3 契约 / 目标端规范条款。>
- <例：全局跨组件层级只用五层 z token；状态标签色阶与规范映射表一致；B3 包络 `code=0` 才取 data。>

## 实际执行命令与结果

- <实际跑了什么命令，通过多少、失败多少。禁止用无关全量命令冒充目标验证。>
- <例：`npx vitest run <spec>` 通过 N 例；`npx eslint <path>` 0 error；`npm run type-check` 0 error。>

## 未运行项

- <明确哪些没跑、为什么（环境限制 / 非本变更链路 / 已由 husky + lint-staged 兜底）。禁止把未执行写成通过。>
- <例：全量 `npm run build` 受本机 dist 清空守卫限制未跑（`npx vite build --emptyOutDir=false` 可验编译）。>

## 截图证据

- <L3 / L4 结论的必要附件：UI 改动附页面截图、关键验证附终端输出快照。截图随本报告置于同目录（引用文件名，不内嵌二进制），须能佐证"验收口径"逐条达成。>

## 结论

- <一句话结论：达成 / 未达成，遗留项与建议（如另立变更修复与本改动无关的既有缺陷）。>
