# 设计文档：<变更名称>

> 适用：L3 / L4。记录架构决策、取舍、风险与依赖。L1 / L2 不建此文件。
> 与本变更 `proposal.md` / `tasks.md` / `spec-delta.md` 四者闭环，人工确认后才动手（AGENTS.md §1.1 / 用户规则 §5）。

## 目标与约束

- <设计目标：要解决的可验证问题，能被 tasks.md 验收标准逐条印证。>
- <硬约束：来自 AGENTS.md §3 API 契约、§6 红线、目标端 UI 规范文档的不可妥协项。>

## 架构与方案

- <分层 / 目录落点：按功能模块为第一级；`services` 与 `adapter` 分离；跨端公共服务置于 `src/`，端内私有服务置于 `apps/<end>/`。>
- <关键组件 / 组合式 / 服务边界与职责，避免重造既有能力（§6.5 优先复用）。>
- <数据流与状态：Pinia store / composable / 桥接层调用路径；移动端只允许经 `apps/mobile/bridges` 访问原生能力。>
- <主题与 token：三端不迁移视觉语言；状态 / 报警 / 设备状态着色只走规范映射表；状态标签用全局 `.tag-*` 类。>

## 决策记录（ADR）

- 决策 1：<选型 X 而非 Y> — 理由：<...> — 反对项：<...>
- 决策 2：<...>

## 风险与缓解

| 风险                         | 可能影响 | 缓解措施                               |
| ---------------------------- | -------- | -------------------------------------- |
| <如：回归 `src/screen` 存量> | <...>    | <...，存量补位走 variables.css 源体系> |
| <如：跨端协议改动>           | <...>    | <...，先走 L4 人工确认>                |

## 依赖

- <上游依赖：设计规范、接口契约、其他 Change（openspec/changes/<name>）。>
- <下游影响：依赖本能力的页面 / 子应用 / 桥接层。>
- <待确认项：标记 `#TODO-确认` 的未决点（如移动端 hybrid 集成方式），不得默认为已决。>
