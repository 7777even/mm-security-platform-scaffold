# Tasks

- [x] 引入 `deleteEmergencyEvent` / `reportAudit` / `useAuthStore` 与 feedback 确认组件
- [x] 新增 `isAdmin` computed（`role === 'ADMIN' || roles.includes('ADMIN')`）
- [x] 实现 `removeAlarm`：确认 → 删除 → 审计 → 刷新
- [x] 列表新增「操作」列，按 `isAdmin` 显隐删除按钮
- [x] 门禁 `type-check` / `eslint` 通过
